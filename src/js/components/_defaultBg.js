if (document.querySelector('.contact, .content-start, .directions, .products-items, .system-integration-hero, .company-hero')) {
  
  $('.out').css('background', '#eaeff2');
  $('.bg-gradient, .bg-monitoring').css('height', 'auto');
}
